# Copy this file to .env and fill in real values.
# .env is gitignored and must never be committed.

# YouTube Data API v3 key used by /api/playlist and /api/video.
YOUTUBE_API_KEY=

# Port the Express server listens on (defaults to 3000).
PORT=3000
